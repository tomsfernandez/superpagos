import spected from 'spected';

/*export const validateFormData = data => {
  const values = Object.values(spected(validationRules, data));
  const errors = values.filter(x => x !== true);
  return errors.flatMap(x => x)
};*/
